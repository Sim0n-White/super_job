json.extract! work, :id, :strukt, :company, :name, :surname, :email, :phone, :needs, :cominfo, :metroid, :street, :house, :payment, :valuta, :created_at, :updated_at
json.url work_url(work, format: :json)
